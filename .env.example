# Full Company Installation - Environment Configuration
# Copy this file to .env and customize before running docker-compose up

# ============================================
# DOMAIN CONFIGURATION (REQUIRED)
# ============================================
# Base domain for all services
# For local dev: use .localhost (self-signed certs)
# For production: use your actual domain (Let's Encrypt certs)
BASE_DOMAIN=example.com

# SSL Configuration - Email for Let's Encrypt notifications
SSL_EMAIL=admin@example.com

# Individual service subdomains
ODOO_SUBDOMAIN=odoo
OPENSIGN_SUBDOMAIN=opensign
NEXTCLOUD_SUBDOMAIN=nextcloud
MATTERMOST_SUBDOMAIN=mattermost
PORTAINER_SUBDOMAIN=portainer

# ============================================
# ODOO CONFIGURATION
# ============================================
POSTGRES_DB=postgres
POSTGRES_USER=odoo
POSTGRES_PASSWORD=CHANGE_THIS_PASSWORD

# ============================================
# OPENSIGN CONFIGURATION
# ============================================
# MongoDB connection (internal Docker network - don't change unless you know what you're doing)
MONGODB_URI=mongodb://opensign-mongo:27017/opensign

# Application ID and keys (CHANGE THESE for production!)
# Generate secure keys with: openssl rand -hex 32
OPENSIGN_APPID=opensignappid
OPENSIGN_MASTERKEY=CHANGE_THIS_MASTERKEY
OPENSIGN_JAVASCRIPTKEY=CHANGE_THIS_JSKEY
OPENSIGN_RESTKEY=CHANGE_THIS_RESTKEY
OPENSIGN_FILEKEY=CHANGE_THIS_FILEKEY

# ============================================
# NEXTCLOUD CONFIGURATION
# ============================================
NEXTCLOUD_ADMIN_USER=admin
NEXTCLOUD_ADMIN_PASSWORD=CHANGE_THIS_PASSWORD
NEXTCLOUD_DB_PASSWORD=CHANGE_THIS_PASSWORD
NEXTCLOUD_DB_ROOT_PASSWORD=CHANGE_THIS_PASSWORD

# ============================================
# MATTERMOST CONFIGURATION
# ============================================
MATTERMOST_DB_PASSWORD=CHANGE_THIS_PASSWORD

# ============================================
# OTHER SETTINGS
# ============================================
# Docker network name
DOCKER_NETWORK=company_network

# Timezone (see: https://en.wikipedia.org/wiki/List_of_tz_database_time_zones)
TZ=Europe/Madrid

# Backup retention (days)
BACKUP_RETENTION_DAYS=7
